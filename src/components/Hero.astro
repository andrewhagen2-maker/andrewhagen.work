---
// Hero.astro — Phase 2
---

<section id="hero">
  <div class="hero-inner container">

    <!-- Text block -->
    <div class="hero-text">
      <p class="hero-eyebrow">Portfolio</p>
      <h1 class="hero-name">Andrew<br />Hagen</h1>
      <p class="hero-title">Compliance &amp; Brand Protection</p>
      <p class="hero-desc">
        Building tools for marketplace enforcement,<br />
        distribution hygiene, and channel integrity.
      </p>
      <a href="#projects" class="hero-cta">View projects ↓</a>
    </div>

    <!-- Geometric SVG accent — yin-yang in palette colors, flat hard-edged -->
    <div class="hero-art" aria-hidden="true">
      <svg
        viewBox="0 0 400 400"
        xmlns="http://www.w3.org/2000/svg"
        class="hero-svg"
      >
        <!--
          Yin-yang construction — all pure geometry, no curves library needed:
          - Outer circle: full symbol boundary
          - Left half (charcoal): left semicircle
          - Top half-lune (terracotta): small top semicircle adds terracotta to top of charcoal side
          - Bottom half-lune (charcoal): small bottom semicircle cuts back into terracotta side
          - Small dot top (terracotta): dot inside charcoal lune
          - Small dot bottom (charcoal): dot inside terracotta lune
          Built using clipPath to keep it crisp and mathematically exact.
        -->

        <defs>
          <!-- Clip to left half of the full circle -->
          <clipPath id="left-half">
            <rect x="0" y="0" width="200" height="400" />
          </clipPath>
          <!-- Clip to right half -->
          <clipPath id="right-half">
            <rect x="200" y="0" width="200" height="400" />
          </clipPath>
          <!-- Clip to top half -->
          <clipPath id="top-half">
            <rect x="0" y="0" width="400" height="200" />
          </clipPath>
          <!-- Clip to bottom half -->
          <clipPath id="bottom-half">
            <rect x="0" y="200" width="400" height="200" />
          </clipPath>
        </defs>

        <!-- Base: full circle in terracotta (right/light side base) -->
        <circle cx="200" cy="200" r="180" fill="#C05A42" />

        <!-- Left half: charcoal -->
        <circle cx="200" cy="200" r="180" fill="#2A2724" clip-path="url(#left-half)" />

        <!-- Top small semicircle: terracotta bulges into charcoal side (top) -->
        <circle cx="200" cy="110" r="90" fill="#C05A42" clip-path="url(#top-half)" />

        <!-- Bottom small semicircle: charcoal bulges into terracotta side (bottom) -->
        <circle cx="200" cy="290" r="90" fill="#2A2724" clip-path="url(#bottom-half)" />

        <!-- Small dot top: terracotta pip inside the charcoal lune -->
        <circle cx="200" cy="110" r="28" fill="#C05A42" />

        <!-- Small dot bottom: charcoal pip inside the terracotta lune -->
        <circle cx="200" cy="290" r="28" fill="#2A2724" />
      </svg>
    </div>

  </div>
</section>

<style>
  #hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    overflow: hidden;
  }

  .hero-inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: center;
    gap: var(--space-lg);
    width: 100%;
    max-width: 1100px;
  }

  /* ── Text ── */

  .hero-text {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .hero-eyebrow {
    font-size: var(--size-xs);
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--color-subtle);
  }

  .hero-name {
    font-size: clamp(3rem, 7vw, 5.5rem);
    font-weight: 500;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    line-height: 1.0;
    color: var(--color-text);
  }

  .hero-title {
    font-size: var(--size-sm);
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--accent-1);
    margin-top: var(--space-xs);
  }

  .hero-desc {
    font-size: var(--size-base);
    color: var(--color-subtle);
    line-height: 1.7;
    max-width: 340px;
    margin-top: var(--space-xs);
  }

  .hero-cta {
    display: inline-block;
    margin-top: var(--space-sm);
    font-size: var(--size-xs);
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--color-text);
    text-decoration: none;
    border-bottom: 1px solid var(--accent-1);
    padding-bottom: 2px;
    width: fit-content;
  }

  .hero-cta:hover {
    color: var(--accent-1);
  }

  /* ── SVG Art ── */

  .hero-art {
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .hero-svg {
    width: 100%;
    max-width: 280px;
    height: auto;
    display: block;
  }

  /* ── Mobile ── */

  @media (max-width: 640px) {
    .hero-inner {
      grid-template-columns: 1fr;
      padding-top: var(--space-xl);
    }

    .hero-art {
      order: -1;
    }

    .hero-svg {
      max-width: 180px;
    }

    .hero-name {
      font-size: clamp(2.5rem, 12vw, 3.5rem);
    }
  }
</style>
