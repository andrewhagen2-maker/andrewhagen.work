---
// Contact.astro — Phase 4 (links wired in Phase 2)
---

<section id="contact">
  <div class="container">
    <p class="section-label">Contact</p>
    <div class="links">
      <a href="https://www.linkedin.com/in/andrewehagen" target="_blank" rel="noopener noreferrer">
        LinkedIn
      </a>
      <a href="https://github.com/andrewhagen2-maker" target="_blank" rel="noopener noreferrer">
        GitHub
      </a>
    </div>
  </div>
</section>

<style>
  #contact {
    padding-top: var(--space-md);
    padding-bottom: var(--space-lg);
  }

  .section-label {
    font-size: var(--size-xs);
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--color-subtle);
    margin-bottom: var(--space-md);
  }

  .links {
    display: flex;
    gap: var(--space-md);
    flex-wrap: wrap;
  }

  a {
    font-size: var(--size-sm);
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--color-text);
    text-decoration: none;
    border-bottom: 1px solid var(--color-subtle);
    padding-bottom: 2px;
  }

  a:hover {
    color: var(--accent-1);
    border-bottom-color: var(--accent-1);
  }
</style>
